
name: 'nyc_taxi_pipeline'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  nyc_taxi_pipeline:
    staging:
      +schema: staging
      +materialized: view
    mart:
      +schema: mart
      +materialized: table
    example:
      +enabled: false